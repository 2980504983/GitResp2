"""
    位运算:
        将整数转换为二进制，按二进制位进行运算

        & 按位与:
            有0则0
        | 按位或:
            有1则1
        ^ 按位异或:
            相同0不同1
        << 左移:
            向左移动低位补0(左移几位补几个0)
        >> 右移:
            向右移动去掉低位(右移几位去掉几位)
"""
